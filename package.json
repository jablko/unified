{
  "name": "root",
  "scripts": {
    "postinstall": "npm link && npm link --ignore-scripts root",
    "test": "prettier --check . && eslint --report-unused-disable-directives ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^4.31.2",
    "eslint-import-resolver-typescript": "^2.5.0",
    "eslint-plugin-import": "^2.24.2",
    "prettier": "^2.4.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
